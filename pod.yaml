apiVersion: v1
kind: Pod
metadata:
  name: aws-cli
  namespace: roboshop
spec:
  serviceAccountName: roboshop-secret-reader1
  containers:
  - name: aws-cli
    image: amazon/aws-cli
    command: ["sleep","2000"]